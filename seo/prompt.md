你是一名资深 SEO + CRO 文案与信息架构专家，同时具备产品营销与编辑能力。

页面背景与固定约束：
- 这是网站首页，目标关键词为：remove people from photos（注意拼写与自然变体）。
- 当前阶段不需要内链模块与内链建议。
- 产品主线能力：Brush 手动涂抹移除人物（核心主卖点与主叙事）。
- 差异化亮点：移除完成后可“继续编辑”（生成式编辑能力，用于进一步改姿势、换背景、修补细节等），这是与传统移除工具的关键区别；页面中要作为独立亮点模块清晰表达，但不要让用户误解“移除质量依赖继续编辑”。
- 页面目标：更匹配搜索意图、更高转化、更清晰的差异化、更强可信度与可读性；避免夸大承诺。

我会提供三份输入材料：
A）现有网页的 SEO 文案与完整结构（包含：标题层级、各模块文案、CTA、FAQ 等）
B）用户需求分析文档（动机→任务→质量标准→常见附加要求）
C）用户原始需求分析文档（更贴近用户真实口语/场景/限制/情绪）

你的任务：基于 A/B/C，重做首页的信息架构与最终可上线文案（英文为主；如 A 是中文则转为英文输出），确保围绕关键词 remove people from photos 的搜索意图构建一条完整的说服链。

工作步骤与要求：
1）理解与提炼
- 从 B/C 提炼：主搜索意图+次意图、用户最在意的 3–7 个价值点、最常见的顾虑/反对意见、必须覆盖的质量标准（例如：自然、无痕、边缘细节、复杂背景、速度、隐私等）。
- 用 5–10 条要点总结“用户真正想要的结果”，并标注应该落在页面哪个区域（Hero/流程/卖点/FAQ/信任/定价附近等）。

2）SEO 结构重构（首页）
- 给出新版页面模块大纲（从上到下顺序），并标注 H1/H2/H3 层级。
- 每个模块说明：模块目标、要传达的关键点、建议字数范围、建议配示例类型（仅描述，不生成图片）。
- 必须包含并优化的模块：Hero、三步流程/How it works、核心卖点（以 Brush 移除为主）、质量与效果说明、案例展示引导、独立的“Continue editing”亮点模块、FAQ、信任与隐私/合规、强 CTA 收口。
- 结构原则：先证明“Brush 移除本身就能高质量完成”，再强调“移除后还能继续编辑带来额外价值”；避免任何会暗示“需要继续编辑才能移除干净”的措辞。

3）输出最终可用文案（英文，重点）
- 输出每个模块的最终文案：标题（标注 H1/H2/H3）、正文、要点列表、按钮文案（主 CTA/次 CTA）、必要的微文案（如上传提示、隐私提示、支持格式提示等）。
- 语气：清晰、可信、偏美国互联网产品风格（直接、具体、少空话），不夸大。
- 覆盖：用户质量标准与常见附加要求；明确的隐私/数据处理表述（稳妥、低风险）；边界条件/失败场景在 FAQ 里说明清楚。
- 生成 8–12 条 FAQ：问题要贴近 C 中的真实表达（更像用户提问），并尽量覆盖：复杂背景、多人、遮挡、边缘细节、分辨率、处理时间、是否需要登录/付费、隐私、商用、移动端等。

4）关键词策略（不做内链）
- 给出关键词策略：主关键词 1 个（remove people from photos），自然变体 5–10 个（例如 remove a person from a photo, remove people from pictures 等），以及 8–12 个长尾问句（用户提问形式）。
- 给出关键词落点建议：哪些变体更适合放在 H2、FAQ、流程模块、信任模块等（不要求密度数字，强调自然可读）。

5）差异化与证据表达
- 基于 B/C，输出 3–6 条“对比传统移除工具”的差异化表述，并落到对应模块的具体句子里（不要停留在口号）。
- “Continue editing”模块必须做到：解释它能解决哪些移除之后常见的进一步需求（如补背景、改姿势、替换背景、微调细节），并强调这是可选的加值能力，而非移除的必需步骤。
- 给出“可信度要素清单”：如示例说明方式、支持格式、隐私声明、退款/试用策略的安全通用文案（如果 A 有则沿用并优化措辞；若没有则给出不虚构数据的通用版本）。

输出格式（必须遵守）：
- 先输出：Assumptions（如有，≤5 条）
- 再输出：新版页面模块大纲（带 H1/H2/H3）
- 再输出：完整最终文案（按模块顺序逐段输出，带标题层级标记）
- 再输出：Keyword plan（主关键词 + 变体 + 长尾问句 + 落点建议）
- 最后输出：Changes vs A（删/加/重排与原因）

限制：
- 不要编造不可验证的数据或背书（如“10M users”“99% accuracy”“#1”）。
- 不要做内链模块与内链建议。
- 若 A/B/C 冲突，以 B/C 的用户真实需求优先；不确定的内容用更稳妥表述或放入 FAQ 解释边界。
- 不要向我反问补充信息；缺失项自行做最佳假设并写在 Assumptions。

现在开始：先阅读我接下来提供的 A/B/C 三份内容，再按上述格式输出结果。


1. Brush removal that doesn’t look fake at 100% 这个模块我觉得可以往后放，而是尽快给到用户效果展示，而不是教用户怎么技巧
2. scenarios和gallery我觉得可以合并成一个模块
 - 采用左边文字描述，右边图片展示，加上 before/after slider 效果
 - 下一个就可以是左边图片，右边文字循坏
 - 这样用户可以更加方便的对比
3. Re-frame after removal这个我觉得应该放在Continue editing模块
4. Continue editing 模块的图片展示，也可以采用左边文字描述，右边图片展示，加上 before/after slider 效果
5. Tips for the cleanest result我觉得可以不要，因为已经有Brush removal that doesn’t look fake at 100%模块



你是一名资深 SEO + CRO 文案与信息架构专家，同时具备产品营销与编辑能力。

  页面背景与固定约束：
  - 这是网站首页，目标关键词为：remove people from photos（注意拼写与自然变体）。
  - 当前阶段不需要内链模块与内链建议。
  - 产品主线能力：Brush 手动涂抹移除人物（核心主卖点与主叙事）。
  - 差异化亮点：移除完成后可“继续编辑”（生成式编辑能力，用于进一步改姿势、换背景、修补细节等），这是与传统移除工具的关键区别；页面中要作为独立亮点模块清晰表达，但不要让用户误解“移除质量依赖继续编辑”。
  - 页面目标：更匹配搜索意图、更高转化、更清晰的差异化、更强可信度与可读性；避免夸大承诺。

  @seo/reddit-case-analysis.md @seo/reddit-requirment-analysis.md 这是两份 reddit真实用户需求分析，这是我目前的文案 @seo/seo-copy-final.md ，这是Google 搜索排名前四的竞品分析 @seo/competitive-analysis.md ,
  根据这些资料，帮我分析一下，看下我的 seo-copy-final 是否有需要优化的地方，只分析，不做任何修改。

